# Godot-generated cache — never commit
.godot/

# Imported asset cache
*.import
*.translation

# Export presets (contain platform credentials)
export_presets.cfg

# Mono / C# build artifacts
.mono/
data_*/
mono_crash.*.json

# Ziva — installer binaries and the full installed agent (reinstallable)
ziva_installer/bin/
addons/ziva_agent/

# OS junk
.DS_Store
Thumbs.db
desktop.ini
